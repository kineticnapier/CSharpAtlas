# Static article data

These JSON files are served directly by the static site. Keep this directory in sync with the source article data under `../content` while the ASP.NET Core development host remains in the repository.
