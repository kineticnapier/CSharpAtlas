{
  "program-entry": "Console.WriteLine(\"Hello, C#!\");",
  "variables-assignment": "int count = 3;\ncount += 2;",
  "numeric-types": "int count = 42;\ndouble ratio = 3.14;\ndecimal price = 19.99m;",
  "boolean-operators": "bool ok = age >= 18 && hasId;",
  "if-else": "if (score >= 90)\n    grade = \"A\";\nelse\n    grade = \"B\";",
  "while-do-loops": "while (count > 0)\n    count--;",
  "methods-parameters-returns": "int Add(int a, int b)\n    => a + b;",
  "arrays-basics": "int[] values = [1, 2, 3];\nConsole.WriteLine(values[0]);",
  "string-literals": "string path = @\"C:\\temp\\file.txt\";\nstring name = $\"user-{id}\";",
  "caller-argument-expression": "void Check(bool ok, [CallerArgumentExpression(\"ok\")] string? expr = null);",
  "argumentnullexception-throwifnull": "ArgumentNullException.ThrowIfNull(value);",
  "collection-expression-syntax": "int[] numbers = [1, 2, 3, 4];",
  "value-reference-types": "int a = 1;\nvar user = new User();",
  "properties": "public string Name { get; init; } = \"\";",
  "required-members": "public required string Name { get; init; }",
  "records": "public record User(string Name, int Age);",
  "record-with-expression": "var older = user with { Age = user.Age + 1 };",
  "interfaces": "interface IShape { double Area { get; } }",
  "generics": "T Echo<T>(T value) => value;",
  "generic-constraints-design": "T Create<T>() where T : new()\n    => new T();",
  "variance-generic-interfaces": "IEnumerable<string> names = [\"A\"];\nIEnumerable<object> objects = names;",
  "equality-comparer-design": "var set = new HashSet<string>(StringComparer.OrdinalIgnoreCase);",
  "delegates": "Func<int, int> square = x => x * x;",
  "events": "public event EventHandler? Changed;\nChanged?.Invoke(this, EventArgs.Empty);",
  "pattern-matching": "if (value is string text)\n    Console.WriteLine(text.Length);",
  "ref-out-in-parameters": "void Swap(ref int a, ref int b)\n    => (a, b) = (b, a);",
  "span-vs-memory": "Span<int> span = values.AsSpan();\nMemory<int> memory = values.AsMemory();",
  "ref-struct-lifetime": "Span<byte> buffer = stackalloc byte[128];",
  "collections-marshal-value-ref": "ref int value = ref CollectionsMarshal.GetValueRefOrAddDefault(map, key, out _);\nvalue++;",
  "iparsable-generic-parsing": "T Parse<T>(string s) where T : IParsable<T>\n    => T.Parse(s, null);",
  "list-basic": "var items = new List<int> { 1, 2, 3 };\nitems.Add(4);",
  "hashset-basic": "var seen = new HashSet<int>();\nseen.Add(id);",
  "ienumerable": "IEnumerable<int> values = GetValues();\nforeach (var x in values) { }",
  "lambda": "Func<int, bool> even = x => x % 2 == 0;",
  "list-filter": "var adults = users.FindAll(u => u.Age >= 18);",
  "linq-select": "var names = users.Select(u => u.Name);",
  "linq-deferred": "var query = users.Where(u => u.Active);\nvar result = query.ToList();",
  "enumerable-chunk-batching": "foreach (var batch in items.Chunk(100))\n    Process(batch);",
  "linq-groupby": "var byTeam = users.GroupBy(u => u.Team);",
  "linq-countby": "var counts = words.CountBy(word => word);",
  "linq-aggregateby": "var totals = orders.AggregateBy(o => o.CustomerId, 0m, (sum, o) => sum + o.Total);",
  "linq-distinct": "var unique = values.Distinct();",
  "enumerable-distinctby": "var unique = users.DistinctBy(u => u.Id);",
  "enumerable-maxby": "var best = users.MaxBy(u => u.Score);",
  "enumerable-minby": "var earliest = events.MinBy(e => e.Time);",
  "priorityqueue-min-heap": "var pq = new PriorityQueue<string, int>();\npq.Enqueue(\"low\", 10);\npq.Enqueue(\"high\", 1);",
  "frozen-dictionary-read-mostly": "var lookup = source.ToFrozenDictionary(x => x.Id);",
  "frozen-set-read-mostly": "var allowed = names.ToFrozenSet(StringComparer.OrdinalIgnoreCase);",
  "immutablearray-snapshot": "ImmutableArray<int> snapshot = items.ToImmutableArray();",
  "dictionary-ensure-capacity": "map.EnsureCapacity(expectedCount);",
  "alternate-lookup-span-key": "var lookup = map.GetAlternateLookup<ReadOnlySpan<char>>();\nlookup.TryGetValue(keySpan, out var value);",
  "trygetnonenumeratedcount": "if (source.TryGetNonEnumeratedCount(out int count))\n    Console.WriteLine(count);",
  "array-binarysearch-sorted": "int index = Array.BinarySearch(sorted, target);",
  "concurrent-dictionary-update": "map.AddOrUpdate(key, 1, (_, value) => value + 1);",
  "try-catch": "try { Save(); }\ncatch (IOException ex) {\n    Log(ex);\n}",
  "exception-filters": "catch (HttpRequestException ex) when (ex.StatusCode == HttpStatusCode.NotFound) { }",
  "exception-dispatch-info-rethrow": "var edi = ExceptionDispatchInfo.Capture(ex);\nedi.Throw();",
  "nullable": "string? name = FindName();\nif (name is not null)\n    Console.WriteLine(name.Length);",
  "nullable-value-types": "int? count = null;\nint value = count ?? 0;",
  "configureawait-suppress-throwing": "await task.ConfigureAwait(ConfigureAwaitOptions.SuppressThrowing);",
  "idisposable": "using var stream = File.OpenRead(path);",
  "iasyncdisposable-await-using": "await using var stream = File.OpenRead(path);",
  "file-write": "File.WriteAllText(\"note.txt\", text);",
  "file-stream-async": "await using var stream = new FileStream(path, FileMode.Open, FileAccess.Read, FileShare.Read, 4096, true);",
  "readexactly-stream": "await stream.ReadExactlyAsync(buffer);",
  "stream-copytoasync": "await input.CopyToAsync(output);",
  "stream-read-partial-buffer": "int read = await stream.ReadAsync(buffer);\nProcess(buffer[..read]);",
  "directory-enumerate": "foreach (var file in Directory.EnumerateFiles(path))\n    Console.WriteLine(file);",
  "file-options-sequentialscan": "using var stream = new FileStream(path, FileMode.Open, FileAccess.Read, FileShare.Read, 4096, FileOptions.SequentialScan);",
  "randomaccess-offset-io": "int read = await RandomAccess.ReadAsync(handle, buffer, offset);",
  "memorymappedfile-view": "using var mmf = MemoryMappedFile.CreateFromFile(path);\nusing var view = mmf.CreateViewAccessor();",
  "file-readlines-streaming": "foreach (var line in File.ReadLines(path))\n    Process(line);",
  "file-enumerate-lines-async": "await foreach (var line in File.ReadLinesAsync(path))\n    Process(line);",
  "json-write": "string json = JsonSerializer.Serialize(value);",
  "json-source-generation": "string json = JsonSerializer.Serialize(value, AppJsonContext.Default.MyType);",
  "jsondocument-dispose": "using JsonDocument doc = JsonDocument.Parse(json);\nvar root = doc.RootElement;",
  "jsonnode-mutable-dom": "JsonNode node = JsonNode.Parse(json)!;\nnode[\"enabled\"] = true;",
  "json-extension-data": "[JsonExtensionData]\npublic Dictionary<string, JsonElement>? Extra { get; set; }",
  "utf8jsonwriter-streaming": "using var writer = new Utf8JsonWriter(stream);\nwriter.WriteStartObject();\nwriter.WriteString(\"name\", name);\nwriter.WriteEndObject();",
  "http-get": "string text = await client.GetStringAsync(url);",
  "httpclient-reuse": "static readonly HttpClient Client = new();\nvar response = await Client.GetAsync(url);",
  "http-completion-responseheadersread": "using var response = await client.GetAsync(url, HttpCompletionOption.ResponseHeadersRead);",
  "json-deserialize-async-enumerable": "await foreach (var item in JsonSerializer.DeserializeAsyncEnumerable<Item>(stream))\n    Process(item);",
  "ziparchive-create-entry": "var entry = archive.CreateEntry(\"data.txt\");\nawait using var output = entry.Open();",
  "file-move-overwrite": "File.Move(source, destination, overwrite: true);",
  "task": "Task<int> work = ComputeAsync();",
  "async-await": "var result = await LoadAsync();",
  "task-whenall-results": "var results = await Task.WhenAll(tasks);",
  "task-wheneach-completion-order": "await foreach (var completed in Task.WhenEach(tasks))\n    Console.WriteLine(await completed);",
  "task-whenany-timeout": "var completed = await Task.WhenAny(work, Task.Delay(timeout));",
  "task-waitasync-timeout": "var result = await work.WaitAsync(timeout);",
  "task-delay": "await Task.Delay(TimeSpan.FromSeconds(1));",
  "task-delay-timeprovider": "await Task.Delay(delay, timeProvider, cancellationToken);",
  "cancellation-token": "await LoadAsync(cancellationToken);",
  "cancellation-timeout": "using var cts = new CancellationTokenSource(timeout);\nawait LoadAsync(cts.Token);",
  "cancellationtokensource-cancelafter": "cts.CancelAfter(TimeSpan.FromSeconds(5));",
  "linked-cancellation-token": "using var linked = CancellationTokenSource.CreateLinkedTokenSource(a, b);",
  "cancellation-token-register-dispose": "using var registration = token.Register(Cleanup);",
  "cancellation-token-throwifrequested": "cancellationToken.ThrowIfCancellationRequested();",
  "semaphore-slim-limit": "await gate.WaitAsync();\ntry { await WorkAsync(); }\nfinally { gate.Release(); }",
  "async-enumerable-streaming": "await foreach (var item in ReadItemsAsync())\n    Process(item);",
  "async-enumerable-withcancellation": "await foreach (var item in source.WithCancellation(token))\n    Process(item);",
  "synchronization-context": "SynchronizationContext? context = SynchronizationContext.Current;",
  "configureawait-library-code": "var data = await LoadAsync().ConfigureAwait(false);",
  "valuetask-guidance": "ValueTask<int> ReadAsync() => new(cachedValue);",
  "valuetask-single-consumption": "ValueTask<int> pending = ReadAsync();\nint value = await pending;",
  "taskcompletionsource-runasync": "var tcs = new TaskCompletionSource<int>(TaskCreationOptions.RunContinuationsAsynchronously);",
  "interlocked-counter": "Interlocked.Increment(ref count);",
  "parallel-foreachasync": "await Parallel.ForEachAsync(items, async (item, ct) =>\n    await ProcessAsync(item, ct));",
  "bounded-channel-producer-consumer": "var channel = Channel.CreateBounded<Item>(100);\nawait channel.Writer.WriteAsync(item);",
  "channel-trywrite-backpressure": "if (!channel.Writer.TryWrite(item))\n    await channel.Writer.WriteAsync(item);",
  "periodic-timer-loop": "using var timer = new PeriodicTimer(TimeSpan.FromSeconds(1));\nwhile (await timer.WaitForNextTickAsync(token))\n    await TickAsync();",
  "periodic-timer-wait": "using var timer = new PeriodicTimer(interval, timeProvider);\nawait timer.WaitForNextTickAsync(token);",
  "string-interpolation": "string message = $\"Hello, {name}!\";",
  "string-split-join": "var parts = text.Split(',');\nstring csv = string.Join(\",\", parts);",
  "date-format": "string text = DateTime.Now.ToString(\"yyyy-MM-dd\");",
  "regex-basic": "bool match = Regex.IsMatch(text, @\"^\\d+$\");",
  "tuples-deconstruction": "var (name, age) = GetUser();",
  "enums-flags": "[Flags]\nenum Access { Read = 1, Write = 2 }\nvar access = Access.Read | Access.Write;",
  "regex-source-generator": "[GeneratedRegex(@\"^\\d+$\")]\nprivate static partial Regex Digits();",
  "searchvalues-repeated-search": "var separators = SearchValues.Create(\",;|\");\nint index = text.AsSpan().IndexOfAny(separators);",
  "composite-format-reuse": "var format = CompositeFormat.Parse(\"{0}: {1}\");\nstring text = string.Format(null, format, name, value);",
  "string-create-formatting": "string text = string.Create(CultureInfo.InvariantCulture, $\"{x:F2}, {y:F2}\");",
  "rune-unicode-scalar": "foreach (Rune rune in text.EnumerateRunes())\n    Console.WriteLine(rune.Value);",
  "convert-tohexstring": "string hex = Convert.ToHexString(bytes);",
  "convert-tryfrombase64string": "if (Convert.TryFromBase64String(text, buffer, out int written))\n    Use(buffer[..written]);",
  "bitoperations-popcount": "int bits = BitOperations.PopCount(value);",
  "regex-enumeratematches-span": "foreach (var match in Regex.EnumerateMatches(text, pattern))\n    Console.WriteLine(match.Index);",
  "regex-match-timeout": "var regex = new Regex(pattern, RegexOptions.None, TimeSpan.FromSeconds(1));",
  "datetimeoffset-conversion": "DateTimeOffset utc = local.ToUniversalTime();",
  "datetime-kind-model": "DateTime utc = DateTime.SpecifyKind(value, DateTimeKind.Utc);",
  "stopwatch-getelapsedtime": "long start = Stopwatch.GetTimestamp();\nTimeSpan elapsed = Stopwatch.GetElapsedTime(start);",
  "guid-create-version7": "Guid id = Guid.CreateVersion7();",
  "operating-system-platform-check": "if (OperatingSystem.IsWindows())\n    UseWindowsFeature();",
  "memoryextensions-split-span": "foreach (var range in text.AsSpan().Split(','))\n    Use(text.AsSpan()[range]);",
  "arraypool-rent-return": "byte[] buffer = ArrayPool<byte>.Shared.Rent(4096);\ntry { Use(buffer); }\nfinally { ArrayPool<byte>.Shared.Return(buffer); }",
  "arraypool-return-finally": "var buffer = ArrayPool<byte>.Shared.Rent(size);\ntry { Work(buffer); }\nfinally { ArrayPool<byte>.Shared.Return(buffer); }",
  "gc-allocate-uninitialized-array": "var buffer = GC.AllocateUninitializedArray<byte>(size);",
  "array-clear-range": "Array.Clear(buffer, 0, length);",
  "sequenceequal-span-comparison": "bool same = left.AsSpan().SequenceEqual(right);",
  "span-slicing": "Span<int> middle = values.AsSpan()[1..^1];",
  "memory-buffer": "Memory<byte> buffer = new byte[4096];\nawait stream.ReadAsync(buffer);",
  "timeprovider-testable-time": "DateTimeOffset now = timeProvider.GetUtcNow();"
}