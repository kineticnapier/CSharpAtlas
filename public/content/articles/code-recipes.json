[
  {
    "id": "list-basic",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var numbers = new List<int> { 10, 20 };\nnumbers.Add(30);\nnumbers.Remove(20);\n\nConsole.WriteLine(numbers.Count);\nConsole.WriteLine(numbers[0]);",
    "related": [
      "index-out-of-range",
      "list-filter",
      "list-sort"
    ]
  },
  {
    "id": "list-filter",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var numbers = new List<int> { 1, 2, 3, 4, 5 };\nvar even = numbers.Where(x => x % 2 == 0).ToList();\n\nConsole.WriteLine(string.Join(\", \", even));",
    "related": [
      "linq-deferred",
      "lambda",
      "list-basic"
    ]
  },
  {
    "id": "list-sort",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var numbers = new List<int> { 3, 1, 2 };\nnumbers.Sort();\n\nvar descending = numbers.OrderByDescending(x => x).ToList();",
    "related": [
      "list-basic",
      "lambda"
    ]
  },
  {
    "id": "hashset-basic",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var ids = new HashSet<int>();\nids.Add(10);\nids.Add(10);\nids.Add(20);\n\nConsole.WriteLine(ids.Count); // 2\nConsole.WriteLine(ids.Contains(20));",
    "related": [
      "linq-distinct",
      "equality"
    ]
  },
  {
    "id": "string-split-join",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string text = \"10 20 30\";\nstring[] parts = text.Split(' ', StringSplitOptions.RemoveEmptyEntries);\nstring joined = string.Join(\", \", parts);\nConsole.WriteLine(joined);",
    "related": [
      "parse-number",
      "string-interpolation"
    ]
  },
  {
    "id": "string-interpolation",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string name = \"Alice\";\nint score = 95;\nConsole.WriteLine($\"{name}: {score}点\");",
    "related": [
      "date-format",
      "string-split-join"
    ]
  },
  {
    "id": "date-format",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "DateTime now = DateTime.Now;\nConsole.WriteLine(now.ToString(\"yyyy-MM-dd HH:mm:ss\"));",
    "related": [
      "string-interpolation",
      "format-exception"
    ]
  },
  {
    "id": "file-write",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string path = \"output.txt\";\nFile.WriteAllText(path, \"Hello, C#!\");",
    "related": [
      "file-read",
      "directory-not-found",
      "io-exception"
    ]
  },
  {
    "id": "directory-enumerate",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "foreach (string path in Directory.EnumerateFiles(\".\", \"*.txt\"))\n{\n    Console.WriteLine(path);\n}",
    "related": [
      "directory-not-found",
      "ienumerable"
    ]
  },
  {
    "id": "json-write",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using System.Text.Json;\n\nvar user = new { Name = \"Alice\", Score = 100 };\nstring json = JsonSerializer.Serialize(user, new JsonSerializerOptions\n{\n    WriteIndented = true\n});\nConsole.WriteLine(json);",
    "related": [
      "json-read",
      "json-exception",
      "file-write"
    ]
  },
  {
    "id": "http-get",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var client = new HttpClient();\nstring text = await client.GetStringAsync(\"https://example.com/\");\nConsole.WriteLine(text.Length);",
    "related": [
      "async-await",
      "uri-format",
      "cancellation-token"
    ]
  },
  {
    "id": "regex-basic",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using System.Text.RegularExpressions;\n\nstring text = \"ID: 12345\";\nMatch match = Regex.Match(text, @\"\\d+\");\nif (match.Success)\n    Console.WriteLine(match.Value);",
    "related": [
      "string-split-join"
    ]
  },
  {
    "id": "task-delay",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "Console.WriteLine(\"start\");\nawait Task.Delay(1000);\nConsole.WriteLine(\"1秒後\");",
    "related": [
      "task",
      "async-await",
      "cancellation-token"
    ]
  },
  {
    "id": "cancellation-token",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var cts = new CancellationTokenSource();\nCancellationToken token = cts.Token;\n\nTask work = Task.Run(async () =>\n{\n    while (true)\n    {\n        token.ThrowIfCancellationRequested();\n        await Task.Delay(100, token);\n    }\n}, token);\n\ncts.Cancel();",
    "related": [
      "operation-canceled",
      "task-canceled",
      "task-delay"
    ]
  },
  {
    "id": "linq-select",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var numbers = new[] { 1, 2, 3 };\nvar squares = numbers.Select(x => x * x).ToArray();\n\nConsole.WriteLine(string.Join(\", \", squares));",
    "related": [
      "list-filter",
      "lambda",
      "linq-deferred"
    ]
  },
  {
    "id": "linq-groupby",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var words = new[] { \"apple\", \"ant\", \"banana\" };\nvar groups = words.GroupBy(x => x[0]);\n\nforeach (var group in groups)\n{\n    Console.WriteLine($\"{group.Key}: {string.Join(\", \", group)}\");\n}",
    "related": [
      "lambda",
      "ienumerable"
    ]
  },
  {
    "id": "linq-distinct",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var numbers = new[] { 1, 1, 2, 3, 3 };\nvar unique = numbers.Distinct().ToArray();\nConsole.WriteLine(string.Join(\", \", unique));",
    "related": [
      "hashset-basic",
      "equality"
    ]
  },
  {
    "id": "try-catch",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "try\n{\n    int value = int.Parse(input);\n    Console.WriteLine(value);\n}\ncatch (FormatException ex)\n{\n    Console.WriteLine($\"数値ではありません: {ex.Message}\");\n}",
    "related": [
      "format-exception",
      "parse-number"
    ]
  },
  {
    "id": "event-data-pattern",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var download = new Download();\nEventHandler<ProgressEventArgs> handler = OnProgress;\ndownload.ProgressChanged += handler;\ndownload.Report(50);\ndownload.ProgressChanged -= handler;\n\nstatic void OnProgress(object? sender, ProgressEventArgs e)\n    => Console.WriteLine($\"{e.Percent}%\");\n\nsealed class Download\n{\n    public event EventHandler<ProgressEventArgs>? ProgressChanged;\n    public void Report(int percent)\n        => ProgressChanged?.Invoke(this, new ProgressEventArgs(percent));\n}\n\nsealed class ProgressEventArgs(int percent) : EventArgs\n{\n    public int Percent { get; } = percent;\n}",
    "related": [
      "events",
      "delegates",
      "idisposable"
    ]
  },
  {
    "id": "queue-stack",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var arrivals = new Queue<string>();\narrivals.Enqueue(\"Ada\");\narrivals.Enqueue(\"Grace\");\nif (arrivals.TryDequeue(out string? next))\n    Console.WriteLine(next); // Ada\n\nvar history = new Stack<string>();\nhistory.Push(\"first\");\nhistory.Push(\"second\");\nif (history.TryPop(out string? latest))\n    Console.WriteLine(latest); // second",
    "related": [
      "list-basic",
      "invalid-operation",
      "ienumerable"
    ]
  },
  {
    "id": "priority-queue",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var jobs = new PriorityQueue<string, int>();\njobs.Enqueue(\"report\", 20);\njobs.Enqueue(\"alert\", 5);\njobs.Enqueue(\"backup\", 20);\n\nwhile (jobs.TryDequeue(out string? job, out int priority))\n    Console.WriteLine($\"{priority}: {job}\");",
    "related": [
      "custom-ordering",
      "queue-stack"
    ]
  },
  {
    "id": "linked-list",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var route = new LinkedList<string>();\nLinkedListNode<string> station = route.AddLast(\"Central\");\nroute.AddBefore(station, \"West\");\nroute.AddAfter(station, \"East\");\nroute.Remove(station);\n\nConsole.WriteLine(string.Join(\" -> \", route));",
    "related": [
      "list-basic",
      "queue-stack"
    ]
  },
  {
    "id": "immutable-collections",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using System.Collections.Immutable;\n\nImmutableArray<string> first = [\"red\", \"blue\"];\nImmutableArray<string> second = first.Add(\"green\");\n\nConsole.WriteLine(first.Length);  // 2\nConsole.WriteLine(second.Length); // 3",
    "related": [
      "readonly-collection-views",
      "record-shallow-copy",
      "arrays-basics"
    ]
  },
  {
    "id": "dictionary-update-insert",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var scores = new Dictionary<string, int>();\nscores.Add(\"Ada\", 90); // initial insertion: succeeds\ntry\n{\n    scores.Add(\"Ada\", 95); // duplicate: throws\n}\ncatch (ArgumentException)\n{\n    Console.WriteLine(\"duplicate rejected\");\n}\n\nbool added = scores.TryAdd(\"Ada\", 95); // duplicate: false\nscores[\"Ada\"] = 95;                    // insert or replace\nbool removed = scores.Remove(\"Ada\", out int oldScore);\n\nConsole.WriteLine($\"{added}, {removed}, {oldScore}\");",
    "related": [
      "dict-safe",
      "key-not-found",
      "dictionary-duplicate-keys"
    ]
  },
  {
    "id": "dictionary-comparers",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var headers = new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase)\n{\n    [\"Content-Type\"] = \"text/plain\"\n};\n\nConsole.WriteLine(headers[\"content-type\"]);\nConsole.WriteLine(headers.TryAdd(\"CONTENT-TYPE\", \"application/json\")); // False",
    "related": [
      "dict-safe",
      "custom-equality-hashing"
    ]
  },
  {
    "id": "hashset-set-operations",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var first = new HashSet<int> { 1, 2, 3 };\nvar second = new HashSet<int> { 3, 4 };\n\nvar union = new HashSet<int>(first);\nunion.UnionWith(second);\nvar common = new HashSet<int>(first);\ncommon.IntersectWith(second);\nvar onlyFirst = new HashSet<int>(first);\nonlyFirst.ExceptWith(second);\n\nConsole.WriteLine(first.Overlaps(second));\nConsole.WriteLine(common.IsSubsetOf(union));",
    "related": [
      "hashset-basic",
      "linq-set-operators",
      "equality"
    ]
  },
  {
    "id": "array-copy-clear",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var source = new[] { new Box(1), new Box(2), new Box(3) };\nvar target = new Box?[3];\nArray.Copy(source, 1, target, 0, 2);\ntarget[0]!.Value = 20; // source[1] is the same Box\nArray.Clear(target, 1, 2); // sets those slots to null\n\nConsole.WriteLine(source[1].Value); // 20\n\nsealed class Box(int value) { public int Value { get; set; } = value; }",
    "related": [
      "arrays-basics",
      "default-values",
      "value-reference-types"
    ]
  },
  {
    "id": "array-binary-search",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string[] names = [\"Ada\", \"grace\", \"Linus\"];\nArray.Sort(names, StringComparer.OrdinalIgnoreCase);\n\nint result = Array.BinarySearch(names, \"Ken\", StringComparer.OrdinalIgnoreCase);\nif (result < 0)\n{\n    int insertionIndex = ~result;\n    Console.WriteLine($\"Insert at {insertionIndex}\");\n}",
    "related": [
      "arrays-basics",
      "custom-ordering",
      "list-sort"
    ]
  },
  {
    "id": "list-removeall",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var numbers = new List<int> { 1, 2, 3, 4, 5, 6 };\nint removed = numbers.RemoveAll(number => number % 2 == 0);\n\nConsole.WriteLine(removed); // 3\nConsole.WriteLine(string.Join(\", \", numbers)); // 1, 3, 5",
    "related": [
      "list-filter",
      "collection-modified-enumeration",
      "lambda"
    ]
  },
  {
    "id": "linq-any-all",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "int[] values = [2, 4, 7];\nbool hasOdd = values.Any(value => value % 2 != 0);\nbool allPositive = values.All(value => value > 0);\n\nint[] empty = [];\nConsole.WriteLine(empty.Any());             // False\nConsole.WriteLine(empty.All(value => false)); // True",
    "related": [
      "list-filter",
      "boolean-operators",
      "ienumerable"
    ]
  },
  {
    "id": "linq-first-single",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var users = new[] { new User(1, \"Ada\"), new User(2, \"Grace\") };\nUser first = users.First(user => user.Id > 0);\nUser only = users.Single(user => user.Id == 2);\nUser? missing = users.SingleOrDefault(user => user.Id == 3);\n\nConsole.WriteLine($\"{first.Name}, {only.Name}, {missing is null}\");\n\nsealed record User(int Id, string Name);",
    "related": [
      "invalid-operation",
      "linq-any-all",
      "nullable-value-types"
    ]
  },
  {
    "id": "linq-aggregate",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string[] words = [\"C#\", \"LINQ\", \"fold\"];\nstring sentence = words.Aggregate(\n    \"Topics:\",\n    (accumulator, word) => $\"{accumulator} {word}\");\n\nstring empty = Array.Empty<string>().Aggregate(\"Topics:\", (text, word) => text + word);\nConsole.WriteLine(sentence);\nConsole.WriteLine(empty); // Topics:",
    "related": [
      "linq-sum-average",
      "lambda",
      "ienumerable"
    ]
  },
  {
    "id": "linq-sum-average",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var orders = new[] { new Order(12), new Order(18) };\nint total = orders.Sum(order => order.Quantity);\ndouble average = orders.Average(order => order.Quantity);\n\nint?[] optional = [10, null, 20];\nConsole.WriteLine(optional.Sum());     // 30\nConsole.WriteLine(optional.Average()); // 15\n\nsealed record Order(int Quantity);",
    "related": [
      "linq-aggregate",
      "overflow-exception",
      "nullable-value-types"
    ]
  },
  {
    "id": "linq-minby-maxby",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var products = new[]\n{\n    new Product(\"Pen\", 3m),\n    new Product(\"Book\", 20m),\n    new Product(\"Bag\", 45m)\n};\n\nProduct cheapest = products.MinBy(product => product.Price)!;\nProduct priciest = products.MaxBy(product => product.Price)!;\nConsole.WriteLine($\"{cheapest.Name} / {priciest.Name}\");\n\nsealed record Product(string Name, decimal Price);",
    "related": [
      "custom-ordering",
      "linq-first-single",
      "list-sort"
    ]
  },
  {
    "id": "linq-selectmany",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var departments = new[]\n{\n    new Department(\"R&D\", [\"Ada\", \"Grace\"]),\n    new Department(\"Ops\", [\"Linus\"])\n};\n\nvar members = departments.SelectMany(\n    department => department.Members,\n    (department, member) => new { department.Name, Member = member });\n\nforeach (var item in members) Console.WriteLine($\"{item.Name}: {item.Member}\");\n\nsealed record Department(string Name, string[] Members);",
    "related": [
      "linq-select",
      "ienumerable",
      "linq-join"
    ]
  },
  {
    "id": "linq-join",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var customers = new[] { new Customer(1, \"Ada\"), new Customer(2, \"Grace\") };\nvar orders = new[] { new Order(1, \"Book\"), new Order(1, \"Pen\"), new Order(3, \"Bag\") };\n\nvar rows = customers.Join(\n    orders,\n    customer => customer.Id,\n    order => order.CustomerId,\n    (customer, order) => new { customer.Name, order.Item });\n\nforeach (var row in rows) Console.WriteLine($\"{row.Name}: {row.Item}\");\n\nsealed record Customer(int Id, string Name);\nsealed record Order(int CustomerId, string Item);",
    "related": [
      "linq-groupjoin",
      "dict-safe",
      "anonymous-types"
    ]
  },
  {
    "id": "linq-groupjoin",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var customers = new[] { new Customer(1, \"Ada\"), new Customer(2, \"Grace\") };\nvar orders = new[] { new Order(1, \"Book\"), new Order(1, \"Pen\") };\n\nvar grouped = customers.GroupJoin(\n    orders, c => c.Id, o => o.CustomerId,\n    (customer, matches) => new { customer, matches });\n\nvar leftJoin = grouped.SelectMany(\n    row => row.matches.DefaultIfEmpty(),\n    (row, order) => new { row.customer.Name, Item = order?.Item ?? \"(none)\" });\n\nforeach (var row in leftJoin) Console.WriteLine($\"{row.Name}: {row.Item}\");\n\nsealed record Customer(int Id, string Name);\nsealed record Order(int CustomerId, string Item);",
    "related": [
      "linq-join",
      "linq-defaultifempty",
      "linq-selectmany"
    ]
  },
  {
    "id": "linq-pagination",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "int pageNumber = 2;\nint pageSize = 3;\nif (pageNumber < 1 || pageSize < 1)\n    throw new ArgumentOutOfRangeException();\n\nint offset = checked((pageNumber - 1) * pageSize);\nvar page = Enumerable.Range(1, 10)\n    .OrderBy(value => value)\n    .Skip(offset)\n    .Take(pageSize)\n    .ToArray();\n\nConsole.WriteLine(string.Join(\", \", page)); // 4, 5, 6",
    "related": [
      "list-sort",
      "linq-deferred",
      "integer-overflow"
    ]
  },
  {
    "id": "linq-chunk",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "foreach (int[] batch in Enumerable.Range(1, 8).Chunk(3))\n{\n    Console.WriteLine($\"{batch.Length}: {string.Join(\", \", batch)}\");\n}\n// lengths: 3, 3, 2",
    "related": [
      "ienumerable",
      "arrays-basics",
      "linq-pagination"
    ]
  },
  {
    "id": "linq-zip",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string[] names = [\"Ada\", \"Grace\", \"Linus\"];\nint[] scores = [90, 85];\n\nforeach ((string name, int score) in names.Zip(scores))\n    Console.WriteLine($\"{name}: {score}\");\n// Linus has no paired output because scores ends first.",
    "related": [
      "tuples-deconstruction",
      "linq-join",
      "ienumerable"
    ]
  },
  {
    "id": "linq-set-operators",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string[] first = [\"Ada\", \"GRACE\", \"Ada\"];\nstring[] second = [\"ada\", \"Linus\"];\nvar comparer = StringComparer.OrdinalIgnoreCase;\n\nvar union = first.Union(second, comparer);\nvar common = first.Intersect(second, comparer);\nvar difference = first.Except(second, comparer);\n\nConsole.WriteLine(string.Join(\", \", union));\nConsole.WriteLine(string.Join(\", \", common));\nConsole.WriteLine(string.Join(\", \", difference));",
    "related": [
      "hashset-set-operations",
      "linq-distinct",
      "equality"
    ]
  },
  {
    "id": "linq-tolookup",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var orders = new[]\n{\n    new Order(1, \"Book\"),\n    new Order(1, \"Pen\"),\n    new Order(2, \"Bag\")\n};\n\nILookup<int, Order> byCustomer = orders.ToLookup(order => order.CustomerId);\nConsole.WriteLine(string.Join(\", \", byCustomer[1].Select(order => order.Item)));\nConsole.WriteLine(byCustomer[99].Count()); // 0\n\nsealed record Order(int CustomerId, string Item);",
    "related": [
      "linq-groupby",
      "dict-safe",
      "linq-deferred"
    ]
  },
  {
    "id": "linq-cast-oftype",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "object?[] mixed = [1, \"two\", null, 3];\n\nint[] integers = mixed.OfType<int>().ToArray(); // 1, 3\nConsole.WriteLine(string.Join(\", \", integers));\n\nIEnumerable<object?> compatible = [\"Ada\", null];\nforeach (string? value in compatible.Cast<string?>())\n    Console.WriteLine(value ?? \"(null)\");",
    "related": [
      "invalid-cast",
      "pattern-matching",
      "ienumerable"
    ]
  },
  {
    "id": "linq-sequenceequal",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string[] first = [\"Ada\", \"Grace\"];\nstring[] second = [\"ada\", \"GRACE\"];\n\nbool sameContents = first.SequenceEqual(second, StringComparer.OrdinalIgnoreCase);\nbool sameReference = ReferenceEquals(first, second);\nConsole.WriteLine($\"{sameContents}, {sameReference}\"); // True, False",
    "related": [
      "equality",
      "reference-equality",
      "linq-set-operators"
    ]
  },
  {
    "id": "linq-defaultifempty",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "int[] values = [];\nint[] supplied = values.DefaultIfEmpty(-1).ToArray();\nint implicitDefault = values.DefaultIfEmpty().Single();\n\nConsole.WriteLine(supplied[0]);        // -1\nConsole.WriteLine(implicitDefault);    // 0\nConsole.WriteLine(new[] { 5 }.DefaultIfEmpty(-1).Single()); // 5",
    "related": [
      "linq-groupjoin",
      "linq-sum-average",
      "default-values"
    ]
  },
  {
    "id": "string-search-replace",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string source = \"mode=TEST; id=42\";\nint start = source.IndexOf(\"id=\", StringComparison.Ordinal);\nif (start >= 0)\n{\n    string idText = source[(start + 3)..];\n    Console.WriteLine(idText);\n}\n\nstring normalized = source.Replace(\"TEST\", \"test\", StringComparison.OrdinalIgnoreCase);\nConsole.WriteLine(source);     // unchanged\nConsole.WriteLine(normalized);",
    "related": [
      "string-comparison",
      "regex-basic",
      "indices-ranges"
    ]
  },
  {
    "id": "string-trim-validation",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "static string NormalizeName(string? input)\n{\n    if (string.IsNullOrWhiteSpace(input))\n        throw new ArgumentException(\"A name is required.\", nameof(input));\n\n    return input.Trim();\n}\n\nConsole.WriteLine(NormalizeName(\"  Ada  \"));",
    "related": [
      "nullable",
      "argument-exception",
      "string-split-join"
    ]
  },
  {
    "id": "stringbuilder",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var builder = new StringBuilder();\nfor (int i = 1; i <= 3; i++)\n{\n    if (builder.Length > 0) builder.Append(\", \");\n    builder.Append(\"item-\").Append(i);\n}\n\nstring result = builder.ToString();\nstring message = $\"Built {result}\";\nConsole.WriteLine(message);",
    "related": [
      "string-interpolation",
      "string-split-join"
    ]
  },
  {
    "id": "unicode-normalization",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string composed = \"café\";\nstring decomposed = \"cafe\\u0301\";\n\nstring left = composed.Normalize(NormalizationForm.FormC);\nstring right = decomposed.Normalize(NormalizationForm.FormC);\nbool equal = string.Equals(left, right, StringComparison.Ordinal);\nConsole.WriteLine(equal); // True",
    "related": [
      "unicode-runes",
      "string-comparison"
    ]
  },
  {
    "id": "span-parse",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string line = \"month=09\";\nReadOnlySpan<char> token = line.AsSpan(6, 2);\n\nif (int.TryParse(token, NumberStyles.None, CultureInfo.InvariantCulture, out int month)\n    && month is >= 1 and <= 12)\n{\n    Console.WriteLine(month);\n}",
    "related": [
      "span-memory",
      "parse-number",
      "culture-sensitive-numbers"
    ]
  },
  {
    "id": "base64",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "byte[] payload = [0x00, 0xFF, 0x10, 0x80];\nstring encoded = Convert.ToBase64String(payload);\nbyte[] recovered = Convert.FromBase64String(encoded);\nConsole.WriteLine(Convert.ToHexString(recovered));\n\ntry\n{\n    Convert.FromBase64String(\"not base64!\");\n}\ncatch (FormatException)\n{\n    Console.WriteLine(\"Invalid Base64 text.\");\n}",
    "related": [
      "encoding-text-bytes",
      "format-exception"
    ]
  },
  {
    "id": "regex-groups",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var regex = new Regex(\n    @\"\\A(?<amount>\\d+)(?:\\s*(?<unit>kg|g))?(?:\\[(?<note>[^]]*)\\])?\\z\",\n    RegexOptions.CultureInvariant,\n    TimeSpan.FromMilliseconds(100));\n\nMatch match = regex.Match(\"250[]\");\nif (match.Success)\n{\n    Group unit = match.Groups[\"unit\"];\n    Group note = match.Groups[\"note\"];\n    Console.WriteLine(unit.Success ? unit.Value : \"(absent)\");\n    Console.WriteLine(note.Success && note.Value.Length == 0 ? \"(empty)\" : note.Value);\n}",
    "related": [
      "regex-basic",
      "parse-number"
    ]
  },
  {
    "id": "regex-replace",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var regex = new Regex(\n    @\"(?<name>[A-Za-z]+):(?<value>\\d+)\",\n    RegexOptions.CultureInvariant,\n    TimeSpan.FromMilliseconds(100));\n\nstring input = \"pen:2 book:5\";\nstring output = regex.Replace(input, match =>\n    $\"{match.Groups[\"name\"].Value}={match.Groups[\"value\"].Value}\");\nConsole.WriteLine(output);",
    "related": [
      "regex-groups",
      "lambda",
      "string-search-replace"
    ]
  },
  {
    "id": "date-parse-exact",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string input = \"2026-04-01 09:30\";\nconst string format = \"yyyy-MM-dd HH:mm\";\n\nif (DateTime.TryParseExact(\n    input, format, CultureInfo.InvariantCulture,\n    DateTimeStyles.None, out DateTime wallClock))\n{\n    Console.WriteLine(wallClock);\n    Console.WriteLine(wallClock.Kind); // Unspecified\n}",
    "related": [
      "date-format",
      "format-exception",
      "datetime-kind"
    ]
  },
  {
    "id": "time-zone-conversion",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "TimeZoneInfo zone = TimeZoneInfo.Local; // chosen by application configuration\nDateTimeOffset instant = DateTimeOffset.Parse(\n    \"2026-04-01T00:30:00+00:00\", CultureInfo.InvariantCulture);\nDateTimeOffset inZone = TimeZoneInfo.ConvertTime(instant, zone);\nConsole.WriteLine(inZone);\n\nDateTime candidate = new(2026, 11, 1, 1, 30, 0, DateTimeKind.Unspecified);\nif (zone.IsInvalidTime(candidate))\n    Console.WriteLine(\"This wall-clock time does not exist in the selected zone.\");\nelse if (zone.IsAmbiguousTime(candidate))\n    Console.WriteLine(\"Choose which offset is intended.\");",
    "related": [
      "datetimeoffset",
      "datetime-kind",
      "dateonly-timeonly"
    ]
  },
  {
    "id": "timespan-arithmetic",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "TimeSpan timeout = TimeSpan.FromSeconds(30);\nTimeSpan extended = timeout + TimeSpan.FromSeconds(15);\n\nDateTimeOffset started = DateTimeOffset.Parse(\n    \"2026-04-01T00:00:00+00:00\", CultureInfo.InvariantCulture);\nDateTimeOffset finished = started.AddMinutes(12);\nTimeSpan elapsed = finished - started;\n\nConsole.WriteLine(extended.TotalSeconds); // 45\nConsole.WriteLine(elapsed.TotalMinutes);  // 12",
    "related": [
      "datetimeoffset",
      "timespan-component-totals",
      "stopwatch"
    ]
  },
  {
    "id": "stopwatch",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var samples = new List<TimeSpan>();\nfor (int i = 0; i < 3; i++)\n{\n    var stopwatch = Stopwatch.StartNew();\n    Thread.SpinWait(10_000);\n    stopwatch.Stop();\n    samples.Add(stopwatch.Elapsed);\n}\n\nforeach (TimeSpan elapsed in samples)\n    Console.WriteLine(elapsed.TotalMilliseconds);",
    "related": [
      "timespan-arithmetic",
      "task-delay"
    ]
  },
  {
    "id": "math-rounding",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "decimal positive = 2.5m;\ndecimal negative = -2.5m;\n\nConsole.WriteLine(Math.Round(positive, MidpointRounding.ToEven));      // 2\nConsole.WriteLine(Math.Round(positive, MidpointRounding.AwayFromZero)); // 3\nConsole.WriteLine(Math.Floor(negative));    // -3\nConsole.WriteLine(Math.Ceiling(negative));  // -2\nConsole.WriteLine(Math.Truncate(negative)); // -2",
    "related": [
      "numeric-conversions",
      "decimal-calculations",
      "floating-equality"
    ]
  },
  {
    "id": "math-clamp",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "double value = 125;\ndouble minimum = 0;\ndouble maximum = 100;\n\nif (!double.IsFinite(value) || !double.IsFinite(minimum) || !double.IsFinite(maximum))\n    throw new ArgumentOutOfRangeException(nameof(value), \"Finite values are required.\");\n\ndouble bounded = Math.Clamp(value, minimum, maximum);\nConsole.WriteLine(bounded); // 100\n// Math.Clamp(value, 10, 0) throws ArgumentException.",
    "related": [
      "numeric-types",
      "argument-exception",
      "nan-infinity"
    ]
  },
  {
    "id": "random-generation",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "var random = new Random(12345);\nint die = random.Next(1, 7);       // 1 through 6; 7 is exclusive\ndouble fraction = random.NextDouble(); // 0.0 <= value < 1.0\n\nvar replay = new Random(12345);\nConsole.WriteLine(die == replay.Next(1, 7)); // True",
    "related": [
      "numeric-types",
      "arrays-basics"
    ]
  },
  {
    "id": "random-shuffle-sampling",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string[] source = [\"A\", \"B\", \"C\", \"D\", \"E\"];\nstring[] pool = source.ToArray(); // owned copy\nRandom.Shared.Shuffle(pool);      // mutates pool\nstring[] sample = pool[..3];\n\nConsole.WriteLine(string.Join(\", \", sample));\nConsole.WriteLine(string.Join(\", \", source)); // original order is unchanged",
    "related": [
      "random-generation",
      "arrays-basics",
      "linq-pagination"
    ]
  },
  {
    "id": "guid",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "Guid created = Guid.NewGuid();\nstring persisted = created.ToString(\"D\");\n\nif (Guid.TryParseExact(persisted, \"D\", out Guid parsed))\n{\n    var values = new Dictionary<Guid, string> { [parsed] = \"stored\" };\n    Console.WriteLine(values[parsed]);\n}",
    "related": [
      "parse-number",
      "format-exception",
      "dictionary-update-insert"
    ]
  },
  {
    "id": "biginteger",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "BigInteger factorial = BigInteger.One;\nfor (int value = 2; value <= 50; value++)\n    factorial *= value;\n\nConsole.WriteLine(factorial);\ntry\n{\n    long fixedWidth = checked((long)factorial);\n}\ncatch (OverflowException)\n{\n    Console.WriteLine(\"The value does not fit in Int64.\");\n}",
    "related": [
      "numeric-types",
      "integer-overflow",
      "overflow-exception"
    ]
  },
  {
    "id": "decimal-calculations",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "decimal unitPrice = 19.95m;\nint quantity = 3;\ndecimal taxRate = 0.10m;\n\ndecimal subtotal = unitPrice * quantity;\ndecimal total = subtotal * (1m + taxRate);\ndecimal charged = Math.Round(total, 2, MidpointRounding.ToEven);\nConsole.WriteLine(charged);",
    "related": [
      "numeric-types",
      "math-rounding",
      "floating-equality"
    ]
  },
  {
    "id": "memory-stream",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var stream = new MemoryStream();\nstream.WriteByte(0x41);\nstream.WriteByte(0x42);\n\nConsole.WriteLine($\"{stream.Length} bytes / capacity {stream.Capacity}\");\nstream.Position = 0;\nConsole.WriteLine(stream.ReadByte()); // 65\n\nbyte[] copy = stream.ToArray();\nConsole.WriteLine(copy.Length);",
    "related": [
      "stream-basics",
      "stream-position",
      "arrays-basics"
    ]
  },
  {
    "id": "binary-reader-writer",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var stream = new MemoryStream();\nusing (var writer = new BinaryWriter(stream, Encoding.UTF8, leaveOpen: true))\n{\n    writer.Write(42);\n    writer.Write(\"Atlas\");\n}\n\nstream.Position = 0;\nusing var reader = new BinaryReader(stream, Encoding.UTF8, leaveOpen: true);\nint number = reader.ReadInt32();\nstring name = reader.ReadString();\nConsole.WriteLine($\"{number}: {name}\");",
    "related": [
      "stream-basics",
      "end-of-stream",
      "encoding-text-bytes"
    ]
  },
  {
    "id": "json-options-contracts",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using System.Text.Json;\nusing System.Text.Json.Serialization;\n\nvar options = new JsonSerializerOptions\n{\n    PropertyNamingPolicy = JsonNamingPolicy.CamelCase,\n    WriteIndented = true\n};\n\nstring json = JsonSerializer.Serialize(new User(\"Ada\", \"secret\"), options);\nConsole.WriteLine(json);\n\nrecord User(\n    [property: JsonPropertyName(\"display_name\")] string Name,\n    [property: JsonIgnore] string Secret);",
    "related": [
      "json-read",
      "json-write",
      "custom-attributes-reflection"
    ]
  },
  {
    "id": "http-response-status",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using System.Net;\n\nusing var response = new HttpResponseMessage(HttpStatusCode.NotFound)\n{\n    Content = new StringContent(\"missing\")\n};\n\nif (response.IsSuccessStatusCode)\n{\n    string body = await response.Content.ReadAsStringAsync();\n    Console.WriteLine(body);\n}\nelse\n{\n    Console.WriteLine($\"HTTP {(int)response.StatusCode} {response.ReasonPhrase}\");\n}",
    "related": [
      "http-get",
      "http-request-exception",
      "try-catch"
    ]
  },
  {
    "id": "task-whenall",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "Task<int> first = Task.FromResult(10);\nTask<int> second = Task.FromResult(20);\nTask<int> third = Task.FromResult(30);\n\nint[] results = await Task.WhenAll(first, second, third);\nConsole.WriteLine(string.Join(\", \", results)); // 10, 20, 30",
    "related": [
      "task",
      "async-await",
      "aggregate-exception"
    ]
  },
  {
    "id": "task-run-cpu",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var cts = new CancellationTokenSource();\nCancellationToken token = cts.Token;\n\nlong sum = await Task.Run(() =>\n{\n    long total = 0;\n    for (int i = 0; i < 100_000; i++)\n    {\n        if ((i & 1023) == 0) token.ThrowIfCancellationRequested();\n        total += i;\n    }\n    return total;\n}, token);\n\nConsole.WriteLine(sum);",
    "related": [
      "task",
      "async-await",
      "cancellation-token"
    ]
  },
  {
    "id": "interlocked",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "int completed = 0;\n\nParallel.For(0, 1_000, _ =>\n{\n    Interlocked.Increment(ref completed);\n});\n\nConsole.WriteLine(Volatile.Read(ref completed)); // 1000",
    "related": [
      "lock-shared-state",
      "static-members"
    ]
  },
  {
    "id": "concurrent-dictionary",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using System.Collections.Concurrent;\n\nvar counts = new ConcurrentDictionary<string, int>(StringComparer.Ordinal);\nstring[] words = [\"red\", \"blue\", \"red\"];\n\nParallel.ForEach(words, word =>\n{\n    counts.AddOrUpdate(word, 1, (_, oldValue) => oldValue + 1);\n});\n\nConsole.WriteLine(counts[\"red\"]); // 2",
    "related": [
      "dict-safe",
      "lock-shared-state",
      "concurrent-collection-factories"
    ]
  },
  {
    "id": "semaphore-async",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var gate = new SemaphoreSlim(2);\nusing var cts = new CancellationTokenSource();\n\nasync Task WorkAsync(int id, CancellationToken token)\n{\n    bool entered = false;\n    try\n    {\n        await gate.WaitAsync(token);\n        entered = true;\n        await Task.Delay(20, token);\n        Console.WriteLine(id);\n    }\n    finally\n    {\n        if (entered) gate.Release();\n    }\n}\n\nawait Task.WhenAll(Enumerable.Range(0, 4).Select(i => WorkAsync(i, cts.Token)));",
    "related": [
      "lock-shared-state",
      "finally-cleanup",
      "cancellation-token"
    ]
  },
  {
    "id": "path-combine-normalize",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string baseDirectory = Path.GetFullPath(Path.Combine(Path.GetTempPath(), \"atlas-base\"));\nstring relativeInput = Path.Combine(\"reports\", \"today.txt\");\n\nif (Path.IsPathRooted(relativeInput))\n    throw new ArgumentException(\"Expected a relative path.\", nameof(relativeInput));\n\nstring fullPath = Path.GetFullPath(Path.Combine(baseDirectory, relativeInput));\nstring relative = Path.GetRelativePath(baseDirectory, fullPath);\nbool escapesBase = relative == \"..\" ||\n    relative.StartsWith($\"..{Path.DirectorySeparatorChar}\", StringComparison.Ordinal) ||\n    Path.IsPathRooted(relative);\n\nif (escapesBase)\n    throw new InvalidOperationException(\"Path escapes the base directory.\");\n\nConsole.WriteLine(fullPath);",
    "related": [
      "file-not-found",
      "path-metadata-extension",
      "directory-not-found"
    ]
  },
  {
    "id": "path-metadata-extension",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string path = Path.Combine(\"reports\", \"summary.tar.gz\");\n\nstring fileName = Path.GetFileName(path);\nstring extension = Path.GetExtension(path);\nstring changed = Path.ChangeExtension(path, \".zip\");\n\nConsole.WriteLine(fileName);   // summary.tar.gz\nConsole.WriteLine(extension);  // .gz\nConsole.WriteLine(changed);    // reports/.../summary.tar.zip",
    "related": [
      "path-combine-normalize",
      "file-copy-move-delete"
    ]
  },
  {
    "id": "directory-create-move-delete",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string root = Path.Combine(Path.GetTempPath(), $\"atlas-{Guid.NewGuid():N}\");\nstring moved = root + \"-moved\";\n\nDirectory.CreateDirectory(root);\ntry\n{\n    File.WriteAllText(Path.Combine(root, \"note.txt\"), \"hello\");\n    Directory.Move(root, moved);\n\n    foreach (string file in Directory.EnumerateFiles(moved))\n        Console.WriteLine(Path.GetFileName(file));\n}\nfinally\n{\n    if (Directory.Exists(root)) Directory.Delete(root, recursive: true);\n    if (Directory.Exists(moved)) Directory.Delete(moved, recursive: true);\n}",
    "related": [
      "directory-enumerate",
      "directory-not-found",
      "unauthorized-access"
    ]
  },
  {
    "id": "file-copy-move-delete",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string root = Path.Combine(Path.GetTempPath(), $\"atlas-{Guid.NewGuid():N}\");\nDirectory.CreateDirectory(root);\n\ntry\n{\n    string source = Path.Combine(root, \"source.txt\");\n    string copy = Path.Combine(root, \"copy.txt\");\n    string moved = Path.Combine(root, \"moved.txt\");\n\n    File.WriteAllText(source, \"data\");\n    File.Copy(source, copy, overwrite: false);\n    File.Move(copy, moved, overwrite: false);\n    File.Delete(moved);\n}\nfinally\n{\n    Directory.Delete(root, recursive: true);\n}",
    "related": [
      "io-exception",
      "file-not-found",
      "path-combine-normalize"
    ]
  },
  {
    "id": "file-lines-streaming",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string path = Path.GetTempFileName();\ntry\n{\n    File.WriteAllLines(path, [\"info\", \"warning: disk\", \"done\"]);\n\n    IEnumerable<string> warnings = File.ReadLines(path)\n        .Where(line => line.StartsWith(\"warning\", StringComparison.Ordinal));\n\n    foreach (string line in warnings)\n        Console.WriteLine(line);\n}\nfinally\n{\n    File.Delete(path);\n}",
    "related": [
      "file-read",
      "ienumerable",
      "linq-deferred"
    ]
  },
  {
    "id": "file-async",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string path = Path.GetTempFileName();\nusing var cts = new CancellationTokenSource(TimeSpan.FromSeconds(5));\nCancellationToken token = cts.Token;\n\ntry\n{\n    await File.WriteAllTextAsync(path, \"hello\", token);\n    string text = await File.ReadAllTextAsync(path, token);\n    Console.WriteLine(text);\n}\nfinally\n{\n    File.Delete(path);\n}",
    "related": [
      "file-read",
      "file-write",
      "async-await",
      "cancellation-token"
    ]
  },
  {
    "id": "streamreader-writer",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var stream = new MemoryStream();\n\nusing (var writer = new StreamWriter(stream, Encoding.UTF8, bufferSize: 1024, leaveOpen: true))\n{\n    writer.WriteLine(\"日本語 text\");\n}\n\nstream.Position = 0;\nusing var reader = new StreamReader(\n    stream, Encoding.UTF8, detectEncodingFromByteOrderMarks: true, bufferSize: 1024, leaveOpen: true);\n\nConsole.WriteLine(reader.ReadLine());",
    "related": [
      "stream-basics",
      "encoding-text-bytes"
    ]
  },
  {
    "id": "stream-copy",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var source = new MemoryStream(Encoding.UTF8.GetBytes(\"copy me\"));\nusing var destination = new MemoryStream();\nusing var cts = new CancellationTokenSource(TimeSpan.FromSeconds(5));\n\nawait source.CopyToAsync(destination, cts.Token);\n\ndestination.Position = 0;\nusing var reader = new StreamReader(destination, Encoding.UTF8);\nConsole.WriteLine(await reader.ReadToEndAsync(cts.Token));",
    "related": [
      "stream-basics",
      "file-async",
      "cancellation-token"
    ]
  },
  {
    "id": "gzip-stream",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "byte[] payload = Encoding.UTF8.GetBytes(\"compress me\");\nusing var compressed = new MemoryStream();\n\nusing (var gzip = new GZipStream(compressed, CompressionLevel.Optimal, leaveOpen: true))\n{\n    await gzip.WriteAsync(payload);\n}\n\ncompressed.Position = 0;\nusing var decompressor = new GZipStream(compressed, CompressionMode.Decompress, leaveOpen: true);\nusing var restored = new MemoryStream();\nawait decompressor.CopyToAsync(restored);\n\nConsole.WriteLine(Encoding.UTF8.GetString(restored.ToArray()));",
    "related": [
      "stream-basics",
      "stream-copy",
      "idisposable"
    ]
  },
  {
    "id": "zip-archive",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "using var archiveStream = new MemoryStream();\n\nusing (var archive = new ZipArchive(archiveStream, ZipArchiveMode.Create, leaveOpen: true))\n{\n    ZipArchiveEntry entry = archive.CreateEntry(\"notes/readme.txt\");\n    using var writer = new StreamWriter(entry.Open(), Encoding.UTF8);\n    writer.Write(\"hello from zip\");\n}\n\narchiveStream.Position = 0;\nusing var readArchive = new ZipArchive(archiveStream, ZipArchiveMode.Read, leaveOpen: true);\nZipArchiveEntry saved = readArchive.GetEntry(\"notes/readme.txt\")!;\nusing var reader = new StreamReader(saved.Open(), Encoding.UTF8);\nConsole.WriteLine(reader.ReadToEnd());",
    "related": [
      "gzip-stream",
      "streamreader-writer",
      "idisposable"
    ]
  },
  {
    "id": "json-document",
    "type": "code",
    "bad": null,
    "good": null,
    "code": "string json = \"{\\\"kind\\\":\\\"point\\\",\\\"x\\\":3}\";\nJsonElement detached;\n\nusing (JsonDocument document = JsonDocument.Parse(json))\n{\n    JsonElement root = document.RootElement;\n    if (root.TryGetProperty(\"x\", out JsonElement x) && x.ValueKind == JsonValueKind.Number)\n        Console.WriteLine(x.GetInt32());\n\n    detached = root.Clone();\n}\n\nConsole.WriteLine(detached.GetProperty(\"kind\").GetString());",
    "related": [
      "json-read",
      "json-exception",
      "idisposable"
    ]
  }
]
