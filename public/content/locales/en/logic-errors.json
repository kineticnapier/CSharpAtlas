{
  "floating-equality": {
    "title": "Comparing floating-point values with ==",
    "short": "Two double values that should be equal can compare as false.",
    "summary": "Because many decimal fractions cannot be represented exactly in binary floating point, tiny rounding errors can remain after calculations.",
    "why": "Values such as 0.1 and 0.2 do not have finite exact representations in double, so their stored values include rounding error.",
    "tips": "Choose a tolerance based on the scale and precision your application actually needs. See [[equality]] for general equality behavior. For money, decimal may be a better fit.",
    "tags": ["double", "floating point", "comparison"],
    "badNotes": {
      "2": "This compares double values directly with =="
    },
    "goodNotes": {
      "3": "Compare whether the difference is within an acceptable tolerance"
    }
  },
  "integer-overflow": {
    "title": "Integer overflow",
    "short": "A large integer calculation can wrap to a different value without throwing.",
    "summary": "In ordinary unchecked integer arithmetic, a result outside the type's range can wrap around. Use [[overflow-exception|checked]] when you want overflow to be detected as an exception.",
    "why": "int has a fixed number of bits, so values beyond its representable range can leave only the low-order bits of the result.",
    "tips": "For calculations that can grow large, decide whether using long is sufficient or whether checked should raise an [[overflow-exception|OverflowException]].",
    "tags": ["int", "overflow", "checked"],
    "badNotes": {
      "2": "The value wraps when it exceeds the int range"
    },
    "goodNotes": {
      "2": "checked detects the overflow"
    }
  },
  "reference-equality": {
    "title": "Confusing reference identity with value equality",
    "short": "Two objects with the same contents may still compare as different objects.",
    "summary": "For a class, which is a [[value-reference-types|reference type]], the default equality behavior may compare whether two references point to the same instance rather than whether their contents match.",
    "why": "A normal class does not treat separately created instances as equal values unless equality is defined appropriately.",
    "tips": "For types that should compare by value, consider [[records|record]] or implementing [[equality|Equals / GetHashCode]].",
    "tags": ["class", "reference", "=="],
    "badNotes": {
      "4": "A normal class compares these as separate instances"
    },
    "goodNotes": {
      "1": "Use record when value-based equality matches the type's meaning"
    }
  },
  "deferred-enumeration": {
    "title": "Enumerating a LINQ query repeatedly",
    "short": "A costly operation runs multiple times even though the same IEnumerable is reused.",
    "summary": "A LINQ sequence with [[linq-deferred|deferred execution]] can run its source pipeline again every time it is enumerated.",
    "why": "Count() and foreach each enumerate query from the beginning, so GetItems or IsValid can run repeatedly.",
    "tips": "Materialize with ToList when repeated enumeration is actually a problem. That does not mean every [[ienumerable|IEnumerable]] should always be materialized.",
    "tags": ["LINQ", "IEnumerable", "deferred execution"],
    "badNotes": {
      "2": "Count() enumerates query once",
      "3": "foreach enumerates the same query again"
    },
    "goodNotes": {
      "1": "ToList materializes the result once"
    }
  },
  "empty-statement-loop": {
    "title": "A stray semicolon creates an empty loop",
    "short": "The loop repeats an empty statement and the following block runs only once.",
    "summary": "A semicolon immediately after while is a complete empty statement, so it becomes the loop body and disconnects the block that visually follows it.",
    "why": "The bounded condition still advances i, but the block has separate [[scope]] and executes after the loop has already finished.",
    "tips": "Remove the semicolon and use braces for the intended body, then verify the number and values of the observed iterations.",
    "tags": ["while", "semicolon", "empty statement"],
    "badNotes": {
      "2": "This semicolon is the entire loop body"
    },
    "goodNotes": {
      "2": "Without the semicolon, the following block belongs to the loop"
    }
  },
  "negative-remainder": {
    "title": "Negative remainders break circular indexing",
    "short": "A negative dividend can produce a negative remainder and therefore an invalid index.",
    "summary": "C# % computes a remainder whose sign follows the dividend; it is not a mathematical modulo operation that always returns a nonnegative result.",
    "why": "With offset -1 and length 3, offset % length is -1, which cannot index the array.",
    "tips": "Require a positive wrap length so the calculation does not encounter [[divide-by-zero|division by zero]], then normalize with (value % length + length) % length.",
    "tags": ["remainder", "modulo", "circular index"],
    "badNotes": {
      "3": "The remainder is -1, not 2",
      "4": "A negative array index fails"
    },
    "goodNotes": {
      "4": "Reject a nonpositive wrap length before using %",
      "5": "The second remainder keeps the normalized index within bounds"
    }
  },
  "mutable-struct-copy": {
    "title": "Updating only a copy of a struct",
    "short": "A local value-type copy changes while the element stored in a collection does not.",
    "summary": "Reading a struct from List<T> returns its value. Mutating that local value cannot reach back into the list unless the changed struct is assigned to the element again.",
    "why": "item receives a copy of items[0], so incrementing item.Value changes only the local variable. The corrected code explicitly replaces the stored element.",
    "tips": "This follows [[value-reference-types|value-type copying]], not a List<T> defect. Prefer immutable structs or make the read-modify-write replacement visible.",
    "tags": ["struct", "copy", "List"],
    "badNotes": {"2": "Reading the value-type element makes a local copy", "3": "Only the copy is incremented"},
    "goodNotes": {"2": "Read the element value into a local", "3": "Change the local copy", "4": "Write the complete changed value back into the list"}
  },
  "record-shallow-copy": {
    "title": "Record with expressions share nested collections",
    "short": "A record copy is new, but referenced mutable children remain shared by default.",
    "summary": "A with expression performs the record's generated shallow copy and then applies listed member changes. Reference-typed components are copied as references rather than recursively cloned.",
    "why": "An empty with expression gives copy a new Order object whose Items still points to the original List<string>, so mutation is visible through both records.",
    "tips": "Understand the generated [[records|with-copy operation]] before choosing independence. Copy nested collections explicitly, as shown, or store immutable children.",
    "tags": ["record", "with", "shallow copy"],
    "badNotes": {"2": "The generated record copy reuses the Items reference", "3": "This mutates the list shared with original"},
    "goodNotes": {"2": "Replace Items with a new list containing the same elements", "3": "Only the copied order's list is mutated"}
  },
  "closure-loop-capture": {
    "title": "Capturing a loop variable gives callbacks the same value",
    "short": "Deferred callbacks observe one for-loop variable after the loop has advanced.",
    "summary": "A closure captures a variable rather than freezing its value. A for loop has one i variable, so callbacks invoked later all read its final value after the loop ends.",
    "why": "Each bad callback closes over the same i, which has become 3 by invocation time. A local declared inside the body creates separate per-iteration storage.",
    "tips": "A [[closures|captured variable]] is observed when the callback runs. Copy the current value into a new local when each deferred action needs its own snapshot.",
    "tags": ["closure", "for loop", "callback"],
    "badNotes": {"2": "All iterations use the same i variable", "3": "The lambda captures i rather than its current value"},
    "goodNotes": {"4": "Each iteration creates a distinct current variable", "5": "The callback captures that iteration's storage"}
  },
  "dictionary-duplicate-keys": {
    "title": "Forgetting a duplicate-key policy in ToDictionary",
    "short": "Two projected rows choose the same key and dictionary creation fails.",
    "summary": "ToDictionary requires every projected key to be unique under its comparer and throws ArgumentException when it encounters a duplicate.",
    "why": "Both rows use key 1. The correction groups them and explicitly chooses Last as the winner before creating one entry per key.",
    "tips": "Use [[linq-groupby|GroupBy]] as the final representation when multiple values per key are valid; otherwise document whether duplicates are rejected or which value wins.",
    "tags": ["ToDictionary", "duplicate key", "GroupBy"]
  },
  "collection-modified-enumeration": {
    "title": "Mutating a collection during enumeration",
    "short": "A structural List change invalidates the active enumerator.",
    "summary": "List<T>.Enumerator tracks the collection version. Adding or removing elements after enumeration begins invalidates that state and a later MoveNext throws InvalidOperationException.",
    "why": "The bad loop removes from the same List it is traversing. RemoveAll performs the removal as one collection operation without an external enumerator.",
    "tips": "The [[ienumerable|enumerator state]] represents one traversal, not a live cursor resilient to structural mutation; collect changes separately or use a suitable bulk operation.",
    "tags": ["List", "enumerator", "mutation"]
  },
  "mutable-hash-keys": {
    "title": "Mutating a hash key makes entries unreachable",
    "short": "Changing equality-relevant state moves a key logically without rehashing it.",
    "summary": "A Dictionary chooses a bucket from the key's hash at insertion. Mutating fields used by equality or hashing can make later lookup inspect a different bucket.",
    "why": "Changing Code from A to B changes the same object's hash without relocating its stored entry. An immutable record key keeps membership stable.",
    "tips": "The [[custom-equality-hashing|hash and equality contract]] must remain consistent for the entire time a key belongs to a Dictionary or HashSet.",
    "tags": ["Dictionary", "mutable key", "GetHashCode"]
  },
  "linq-orderby-reset": {
    "title": "Repeated OrderBy calls replace the primary sort key",
    "short": "A second ordering starts over instead of adding a tie-breaker.",
    "summary": "OrderBy begins a new ordered sequence. Calling it again makes the new key primary and discards the prior key's role in the requested ordering.",
    "why": "ThenBy extends the Group ordering with Rank only inside equal groups, preserving the intended two-key contract.",
    "tips": "Use [[list-sort|OrderBy to begin an order]] and ThenBy or ThenByDescending to extend it with additional keys.",
    "tags": ["LINQ", "OrderBy", "ThenBy"]
  }
}
